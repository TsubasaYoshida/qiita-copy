= render 'menu', active: 'password'

.setting-main
  .setting__form
    -# Qiitaがputだったため、学習も兼ねてあえてputにしている(patchなら明示不要)
    = simple_form_for(@user, url: settings_password_path, method: 'put') do |f|
      = f.input :old_password, input_html: {class: 'setting__form-text'}
      = f.input :password, label: '新しいパスワード', placeholder: false, input_html: {class: 'setting__form-text'}
      = f.button :submit, class: 'setting__form-submit'

.div
  @#{@user.screen_name}

.div
  = @user.items.size
  Items

- @user.items.each do |item|
  .div
    = link_to item.title, "/#{@user.screen_name}/items/#{item.draft.hashid}"
  .div
    = link_to 'delete', "/#{@user.screen_name}/items/#{item.draft.hashid}", method: :delete
